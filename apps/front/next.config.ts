import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
    /* config options here */
    images: {
        domains: ['loremflickr.com', 'picsum.photos', 'avatars.githubusercontent.com', 'cdn.jsdelivr.net'],
    },
}

export default nextConfig
